[package]
name = "bench_binius"
version = "0.1.0"
edition = "2024"

[dependencies]
binius_field = { git = "https://github.com/IrreducibleOSS/binius", package = "binius_field" }
halo2curves = { version = "0.9.0", features = ["derive_serde", "std"] }

[profile.release]
debug = 1
lto = "fat"
